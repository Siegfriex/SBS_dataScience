spring:
  application:
    name: sbs-datascience-api

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true

info:
  app:
    name: sbs-datascience-api
    description: SBS DataScience Spring Boot API
