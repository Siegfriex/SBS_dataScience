# P4 v4.0 SSOT authority

`P4_final_design_v4.0.md` is the project-wide implementation baseline for
M1.5-P through M1.5-D. The copy in this directory is byte-identical to the
user-designated source under `P4_DOCS/.../docs/ssot`.

`P4_CONTRACT_v2.1.2` remains the canonical warehouse contract. The v4.0
semantic/NCS schemas are an implementation addendum and do not silently change
the contract version.

No document in this directory authorizes a production Linkareer crawl,
production-data promotion, article-number generation, or an `ANALYSIS_READY`
claim.
